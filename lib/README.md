# Application layout

- `main.dart`: Flutter entry point.
- `app/`: app shell and theme.

Create `core/cards`, `core/game_rules`, `core/coaching`, `features/`, and
`scenarios/` when their first implementations land. No empty services or feature
controllers are needed for the bootstrap. Authored content remains under
`content/scenarios/v1/`; it is not loaded by this preview.
