name: estimation_coach
description: "A local visual trainer for better Estimation card-game decisions."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  json_schema: ^5.2.2

flutter:
  uses-material-design: true
  assets:
    - content/scenarios/v1/bidding/
